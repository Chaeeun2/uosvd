rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 사용자가 읽기 가능
    match /{document=**} {
      allow read: if true;
    }
    
    // 인증된 사용자만 쓰기 가능
    match /{document=**} {
      allow write: if request.auth != null;
    }
  }
} 